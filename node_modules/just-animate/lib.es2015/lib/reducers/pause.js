import { loopOff } from '../core/timeloop';
import { S_PAUSED, _ } from '../utils/constants';
import { update } from './update';
import { PAUSE } from '../actions';
export const pause = (model, _data, ctx) => {
    model.state = S_PAUSED;
    loopOff(model.id);
    update(model, _, ctx);
    ctx.trigger(PAUSE);
};
