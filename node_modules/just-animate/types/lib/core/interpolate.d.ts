import { Keyframe } from './types';
export declare function interpolate(l: number, r: number, o: number): number;
export declare function interpolator(duration: number, keyframes: Keyframe[]): (timelineOffset: number) => any;
