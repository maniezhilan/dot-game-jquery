Object.defineProperty(exports, "__esModule", { value: true });
var lists_1 = require("../lib/utils/lists");
var inspect_1 = require("../lib/utils/inspect");
var constants_1 = require("./constants");
function appendUnits(effects) {
    for (var propName in effects) {
        if (lists_1.includes(constants_1.cssLengths, propName)) {
            var prop = effects[propName];
            for (var offset in prop) {
                var obj = prop[offset];
                if (inspect_1.isDefined(obj)) {
                    var value = obj.value;
                    if (inspect_1.isNumber(value)) {
                        obj.value += constants_1.PX;
                    }
                }
            }
        }
    }
}
exports.appendUnits = appendUnits;
