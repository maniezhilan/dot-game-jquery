Object.defineProperty(exports, "__esModule", { value: true });
var lists_1 = require("./lists");
var inspect_1 = require("./inspect");
function getTargets(target) {
    return inspect_1.isString(target)
        ? Array.prototype.slice.call(document.querySelectorAll(target))
        :
            inspect_1.isFunction(target)
                ? getTargets(target())
                :
                    inspect_1.isArrayLike(target)
                        ? lists_1.mapFlatten(target, getTargets)
                        :
                            inspect_1.isObject(target)
                                ? [target]
                                :
                                    [];
}
exports.getTargets = getTargets;
