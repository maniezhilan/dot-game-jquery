Object.defineProperty(exports, "__esModule", { value: true });
var math_1 = require("../utils/math");
var constants_1 = require("../utils/constants");
function calculatePlayers(model) {
    model.duration = math_1.max.apply(constants_1._, model.players.filter(function (a) { return isFinite(a.to); }).map(function (a) { return a.to; }));
    model.time = isFinite(model.time) ? model.time : model.rate < 0 ? model.duration : 0;
}
exports.calculatePlayers = calculatePlayers;
